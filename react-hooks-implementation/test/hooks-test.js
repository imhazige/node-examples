import assert from 'assert';
import { describe, it } from 'mocha';
import log from '../../common/log';

describe('Hooks', () => {
  describe('test all ()', () => {
    it('test all', () => {});
  });
});
