# Migration `watch-20200101204747`

This migration has been generated by f at 1/1/2020, 8:47:48 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `prisma2-apollo-test`.`User` (
  `email` varchar(191) NOT NULL DEFAULT '' ,
  `id` varchar(191) NOT NULL  ,
  `name` varchar(191)   ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE `prisma2-apollo-test`.`Post` (
  `content` varchar(191)   ,
  `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `id` varchar(191) NOT NULL  ,
  `published` boolean NOT NULL DEFAULT false ,
  `title` varchar(191) NOT NULL DEFAULT '' ,
  `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

ALTER TABLE `prisma2-apollo-test`.`Post` ADD COLUMN `author` varchar(191)  ,
ADD FOREIGN KEY (`author`) REFERENCES `prisma2-apollo-test`.`User`(`id`) ON DELETE SET NULL;

CREATE UNIQUE INDEX `User.email` ON `prisma2-apollo-test`.`User`(`email`)
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..watch-20200101204747
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,31 @@
+// datasource db {
+//   provider = "sqlite"
+//   url      = "file:dev.db"
+//   default  = true
+// }
+
+datasource mysql {
+    provider = "mysql"
+    url      = "mysql://root:root@localhost:3306/prisma2-apollo-test"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model User {
+  id    String  @default(cuid()) @id
+  email String  @unique
+  name  String?
+  posts Post[]
+}
+
+model Post {
+  id        String   @default(cuid()) @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  published Boolean  @default(false)
+  title     String
+  content   String?
+  author    User?
+}
```


